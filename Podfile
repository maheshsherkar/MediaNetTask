# Podfile
use_frameworks!

target 'OMDB' do
    pod 'Swinject'
    pod 'SDWebImage', '~> 4.0'
end

# RxTest and RxBlocking make the most sense in the context of unit/integration tests
target 'OMDBTests' do
    pod 'Swinject'
end
